{
  "dependencies": {
    "jsdom": "^26.0.0"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "husky": "^9.1.7",
    "lint-staged": "^16.1.2",
    "typescript": "^5.7.2"
  },
  "scripts": {
    "prepare": "husky"
  },
  "lint-staged": {
    "frontend/**/*.{js,ts,jsx,tsx}": [
      "pnpm exec biome check --apply"
    ],
    "backend/**/*.py": [
      "uvx ruff format",
      "uvx ruff check --fix"
    ]
  }
}
